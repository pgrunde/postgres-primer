SELECT name, email FROM users WHERE email = 'user@example.com';

SELECT name, email FROM users WHERE email = 'user@example.com' AND address = '404 Not Found St';

SELECT name, email FROM users WHERE id > 1;
